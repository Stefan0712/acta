import styles from './Settings.module.css';

const Settings = () => {
    return ( 
        <div className={styles.settings}>
            <h2>Settings</h2>
        </div>
     );
}
 
export default Settings;